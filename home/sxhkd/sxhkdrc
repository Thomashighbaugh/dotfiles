#
# simple x hotkey daemon configuration
#

# Common

super + Return
	$TERMINAL
super + F2
	$BROWSER
super + control + escape
	rofi -show drun
super + F3
    $FILE_MANAGER
super + ctrl + F11
	$TERMINAL -e $EDITOR
super + control + F11
	$TERMINAL -e nvim -c VimwikiUISelect

# Multimedia Keys

XF86MonBrightness{Up,Down}
	lightctl {i,d} 10
XF86Audio{Raise,Lower}Volume
	amixer sset Master 5%{+,-}
XF86AudioMute
	amixer sset Master toggle
XF86Audio{Next,Prev}
	mpc {next,prev}
XF86AudioPlay
	mpctoggle


# Power Keys

super + shift + escape
	ask "doas -A poweroff" "Are you sure you want to shutdown?"
super + control + shift + escape
	ask "doas -A reboot" "Are you sure you want to reboot?"
super + mod1 + shift + escape
	ask "doas -A /usr/local/bin/nap" "Do you want to suspend?"

