#!/usr/bin/env bash
alias gitu="git add -u . && git add . && git commit -m 'Committed'  && git push"
alias gitc="git clone "
alias gsub="git submodule "
alias gsuba="git submodule add " 
alias gsubup="git submodule update --remote "
